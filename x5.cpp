#include <bits/stdc++.h>
using namespace std;
double x;
int y;
int main()
{
  cin>>x;
  if(x>0) y++;
  cin>>x;
  if(x>0) y++;
  cin>>x;
  if(x>0) y++;
  cin>>x;
  if(x>0) y++;
  cin>>x;
  if(x>0) y++;
  cin>>x;
  if(x>0) y++;
  cout<<y<<" positive numbers";
  return 0;
}

